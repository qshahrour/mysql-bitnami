name: Deploy Helm Chart to EKS

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Uninstall
        run: helm uninstall /helm
